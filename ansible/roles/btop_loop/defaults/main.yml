---
btop_loop_bin_dir: /usr/local/bin

# The kernel hostname this tool belongs on. Host-specific — see HOST_VARS.md.
btop_loop_expected_hostname: vmi3006760

# The digest this host and every other host running the tool must share. The
# same bytes are installed on the CI pool from this repository's
# ci-runner/k3s/phase2/host-tools/btop-loop and on hp-xubuntu from fabro-hosts
# services/host-tools/. The tool is duplicated across those trees because the
# hosts are provisioned from different places; this pin is what makes a
# divergence visible instead of silent. Changing the tool means changing it in
# all three and re-recording the digest in all three, in one deliberate change.
btop_loop_pinned_sha256: c570ef8abf330b969d91d37319b6c147af30daec6c818f4bd5263e8b40768c27
