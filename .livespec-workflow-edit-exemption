work_item=livespec-dev-tooling-37p0
reason=Adds reusable-release-lane-watch.yml, the shared release-lane watcher every publishing fleet repo will call. The workflow IS the deliverable: the lane watcher exists to run on a cadence in Actions, so it cannot be delivered without a workflow file. Lifted from livespec-overseer (overseer-hgq4wi.15) rather than reimplemented. Carrier R1 depth half of plan fleet-delivery-lane-visibility, epic livespec-n33rwg.
