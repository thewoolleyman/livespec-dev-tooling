work_item=livespec-dev-tooling-37p0
reason=Repoints reusable-release-lane-watch.yml at the package module livespec_dev_tooling.cross_repo.release_lane_watch_runner instead of a .github/scripts copy, so the runner is reachable by per-file coverage and directly tested. Same work-item and same already-approved workflow as the original slice; this narrows the diff rather than widening it.
