work_item=livespec-dev-tooling-3lvl
reason=Wires the factory-built release_lane_issue_runner into reusable-release-lane-watch.yml — adds the issues:write permission and replaces the per-lane annotation loop with one call that reconciles the repo's durable release-lane-red issue (Option B producer for livespec-n33rwg.5). The factory built and greened the Python (release_lane_issue.py + runner + tests, livespec-dev-tooling PR #2131) but cannot push .github/workflows edits; maintainer authorized this workflow-only tail on 2026-09-09.
