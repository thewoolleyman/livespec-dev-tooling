---
proposal: role-key-declaration-required.md
decision: accept
revised_at: 2026-07-25T21:35:14Z
author_human: thewoolleyman <chad@thewoolleyman.com>
author_llm: claude-opus-5-rop-sweep-fleet-policy
---

## Decision and Rationale

Accepted after the enforcing code shipped and was verified fleet-wide. Slices L (PR #633, v0.54.12), D (PR #644, v0.54.13) and C (PR #648, v0.54.14) are merged and all eight pin-carrying fleet repos sit on v0.54.14, so this text describes shipped, fleet-verified behavior rather than intent -- discharging the file-early/ratify-after gate the proposal set for itself. THE EXIT-CODE RULING: the maintainer ruled that contracts.md ratifies exit 4 for no_shadow_ledger_body_identical's missing/body_mismatch failure modes while the shipped module returns 1, so the spec states the correct contract and the CODE carries the tracked defect (livespec-dev-tooling-1aba). A draft had rewritten the spec to match the code; that was REVERTED, leaving those three lines byte-identical to master, which also dissolved the defect that the rewrite was an edit no EDIT declared. Verified from git history: the module shipped 4 at creation (234078e) and was flipped to 1 by slice L (b8ea4e6), whose commit message declares no exit-code change; the flip conflated the undeclared-role-key gate's exit with the distinct body-mismatch exit. CORRECTIONS RECORDED RATHER THAN SILENTLY FIXED: the filed proposal's slice-D scope criterion would have INCLUDED and reddened livespec-console-beads-fabro, the very repo it named as excluded, and is corrected to the shipped 'repos wiring at least one layout-dependent load_config-consuming check'; its EDIT 5 read 'PENDING the maintainer CALL #2 answer', now finalized on the ruling (fleet-conformance placement only, no /livespec:doctor surface); and an enforcement-universality overclaim was corrected to be universal on DECLARATION and precise on ENFORCEMENT, with no reduction of the mandate. FOUND BY INDEPENDENT REVIEW AND FIXED: contracts.md claimed five checks 'no-op against this library' when three of them enforce -- disproved by EXECUTION (no_except_outside_io and no_raise_outside_io each inspect 136 files; no_write_direct emits warnings) -- which also contradicted this change's own gating distinction; the config block's origin was credited twice to livespec PR #1663 when it was added by PR #367 / commit 8f6ecc59 on 2026-05-31, seven weeks earlier, contradicting the same paragraph's own date; and the third consumer bullet named the retired repo livespec-impl-git-jsonl while deferring an obligation this epic's own Wave-1 had already discharged. Five further consistency repairs landed with them: the loader docstring's role-key wording, two over-broad 'loader-recognized keys' claims, the undeclared-key exit missing from the check's invocation line, the sibling-repo console example dated to ratification, and an acknowledgment that the fallback retirement is MAJOR-class by this spec's own definition yet shipped in a PATCH release. TWO DELIBERATE SPEC-AHEAD-OF-CODE DIVERGENCES are recorded in the ratified text: the exit code above (1aba) and newtype_domain_primitives not implementing the declared-non-empty-but-zero-.py error state (livespec-dev-tooling-njyx). In both the contract is correct and the code is the tracked defect. DELIBERATELY OUT OF SCOPE: the 'Three first-party consumers as of v0.2.x' enumeration -- its count, version qualifier, and the first-party-consumer determination -- stays with livespec-dev-tooling-1a6w, which owns it. No '## ' heading is added, renamed or removed -- re-verified after every amendment, and confirmed against heading_coverage.py, which extracts '## ' and excludes '### ' -- so no tests/heading-coverage.json co-edit is required. Independent adversarial review before this accept ran EIGHT rounds across FOUR models (Fable, Opus, Sonnet, plus a second Opus and Sonnet on the final bytes), because each round kept finding real defects. Thirteen distinct defects were found and fixed; SIX of them were introduced by repairs to earlier defects, and none was catchable by CI or the just check aggregate. Two reviewers on identical bytes twice reached OPPOSITE verdicts, which is the concrete argument for model diversity here rather than a single reviewer. The two most serious findings came from different models and different methods: one by EXECUTING the checks (contract text asserted three enforcing checks no-op, disproved by 136 files inspected each), one by cross-repo config resolution (a claim that every backfilled repo declares dataclasses_tree empty, which erased this epic's own headline win in core). FINAL VERDICT: NO-BLOCKERS from both independent reviewers on the final bytes, each independently re-derived rather than deferring to the other, plus the full just check aggregate green at 63/63 targets. The two reviewed files were proven byte-identical across the pre-ratification rebase (contracts.md blob 993002f8, proposal blob 8dfae94e), so the clearance covers exactly the bytes ratified here.

## Resulting Changes

- contracts.md
