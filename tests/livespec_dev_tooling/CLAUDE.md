# tests/livespec_dev_tooling/

Mirrors `livespec_dev_tooling/` one-to-one. Test files at this level
cover modules directly under `livespec_dev_tooling/` (e.g.,
`test_canonical_checks.py` for `canonical_checks.py`); subdirectories
mirror the package's subpackages (`checks/`, `cross_repo/`).
